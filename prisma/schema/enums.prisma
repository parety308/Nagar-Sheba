enum Role {
  CITIZEN
  STAFF
  ADMIN
}

enum AccountStatus {
  ACTIVE
  BLOCKED
}

enum FeeType {
  FREE
  PAID
}

enum RequestStatus {
  PENDING_PAYMENT
  SUBMITTED
  ASSIGNED
  IN_PROGRESS
  RESOLVED
  CLOSED
  CANCELLED
}

enum PaymentProvider {
  BKASH
  STRIPE
  SSLCOMMERZ
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  CANCELLED
  REFUNDED
}

enum AttachmentType {
  EVIDENCE
  RESOLUTION_PROOF
}

enum NotificationChannel {
  EMAIL
}
enum AuthProvider{
  CREDENTIAL
  GOOGLE
}
